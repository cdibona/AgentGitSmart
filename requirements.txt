pygit2>=1.15
Flask>=3.0
python-dotenv>=1.0
pytest>=8.0

# Test harness web UI (testharness/) — see also pyproject.toml [harness] extra
fastapi>=0.111
uvicorn[standard]>=0.29
aiofiles>=23

# System prerequisite (not pip): sudo apt-get install -y universal-ctags  — builds the symbol index; without it symbols are empty.
